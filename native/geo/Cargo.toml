[package]
name = "geo"
version = "0.1.0"
edition = "2021"

[lib]
name = "geo"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
rustler = "0.37"
rayon = "1"

[dev-dependencies]
pprof = { version = "0.13", features = ["flamegraph"] }
