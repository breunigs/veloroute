<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= assigns[:page_title] || Settings.page_title_long() %></title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link href='https://api.mapbox.com/mapbox-gl-js/v1.9.0/mapbox-gl.css' rel='stylesheet' />
    <%= csrf_meta_tag() %>
    <link rel="canonical" href="<%= Settings.url() <> @conn.request_path %>"/>

    <link rel="icon" type="image/png" size="16x16" href="/favicons/16.png">
    <link rel="icon" type="image/png" size="32x32" href="/favicons/32.png">
    <link rel="icon" type="image/png" size="48x48" href="/favicons/48.png">
    <link rel="icon" type="image/png" size="192x192" href="/favicons/192.png">
    <link rel="apple-touch-icon" href="/favicons/57.png">
    <link rel="apple-touch-icon" sizes="152x152" href="/favicons/152.png">
    <link rel="apple-touch-icon" sizes="167x167" href="/favicons/167.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/favicons/180.png">
    <link rel="apple-touch-startup-image" href="/favicons/192.png">
    <link rel="manifest" href="/favicons/manifest.json">
    <%# uncached connections happen, even on repeat view %>
    <link rel="preconnect" href="https://a.mapillary.com">
    <%# connections only happen on initial load, but not on repeat view %>
    <%# <link rel="dns-prefetch" href="https://api.mapbox.com"> # commented out because the CSS already resolves the domain %>
    <link rel="dns-prefetch" href="https://a.tiles.mapbox.com">
    <link rel="dns-prefetch" href="https://b.tiles.mapbox.com">
    <link rel="alternate" type="application/atom+xml" href="/routes/geo/updates.atom" title="veloroute.hamburg – Updates zu Baumaßnahmen und neuen Bildern">
  </head>
  <body>
    <%= @inner_content %>

    <%
      need_polyfill = Plug.Conn.get_req_header(@conn, "user-agent")
        |> List.first()
        |> case do
          ua when is_binary(ua) -> Browser.ie?(ua) || Browser.edge?(ua)
          _ -> false
        end
    %>

    <%= if need_polyfill do %>
      <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/polyfill.js") %>"></script>
    <% end %>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
